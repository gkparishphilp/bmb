%h1
	= @user.name
	Home
- if @welcome_message
	%h3= @welcome_message
%hr
= link_to "I'm an Author!", new_author_path
%br
- unless @current_user.has_email?
	= form_for @current_user do |f|
		= f.label "Email"
		= f.text_field :email
		= f.submit "Enter"


%h3
	Accounts

- for acct in @user.facebook_accounts
	Facebook:
	= acct.name
	%br
	- for fbpg in acct.facebook_pages
		&nbsp;&nbsp;&nbsp;&nbsp;
		= fbpg.page_type
		= fbpg.name
		%br
	%hr

Add a Facebook Account:
#facebook
	= form_tag go_facebook_sessions_path do
		= image_submit_tag 'facebook_connect.png'
	
	
- for acct in @user.twitter_accounts
	Twitter:
	= acct.name
%br
#twitter
	Add a Twitter Accoount
	= form_tag go_twitter_sessions_path do
		= image_submit_tag 'twitter_button.png'

%h3
	Activity
= render @activities
	
	
	
	