<%= form_for(@order_transaction) do |f| %>
  <% if @order_transaction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order_transaction.errors.count, "error") %> prohibited this order_transaction from being saved:</h2>

      <ul>
      <% @order_transaction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
