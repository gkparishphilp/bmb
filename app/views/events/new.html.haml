- content_for :left_column do
	= render 'admin/nav'
- content_for :middle_column do
	.form_group
		%h1 
			New Event
		= form_for [ @admin_author, @event ] do |f|
			= render 'form', :f => f, :m => 'Create'
		
- content_for :right_column do
	= render 'shared/help'
