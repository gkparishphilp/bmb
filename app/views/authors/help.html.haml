- content_for :left_column do
	%h1
		= @author.pen_name
		Help and 
		= link_to "FAQs", "#faq"
		
	%br	
	= render 'default_faq'	if @author.skus.published.count > 0
	= @author.faq.html_safe if @author.faq
	- content_for :right_column do
		&nbsp;
	