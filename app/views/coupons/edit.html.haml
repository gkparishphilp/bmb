- content_for :left_column do
	= render 'admin/nav'
- content_for :middle_column do
	.form_group
		%h1 
			Edit Coupon
		= form_for [ @admin, @coupon ] do |f|
			= render 'form', :f => f, :m => 'Update'
		
- content_for :right_column do
	= render 'shared/help'