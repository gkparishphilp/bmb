- content_for :left_column do
	= render :partial => 'nav'
- content_for :middle_column do
	.form_group
		%h1 Blog
		= draw_admin_header( :article )
		= draw_admin_table( @current_author.articles, [:title, :status, :publish_at] )

- content_for :right_column do
	= render 'shared/help'
	

