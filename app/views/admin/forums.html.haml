- content_for :left_column do
	= render :partial => 'nav'
- content_for :middle_column do
	.form_group
		%h1 
			Manage Forums
		
		= draw_admin_header( :forums )
		= draw_admin_table( @forums, [:title, :description] )
		
- content_for :right_column do
	= render 'shared/help'
