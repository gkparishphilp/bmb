Subject:
%br
=f.text_field :subject
%br

%br
= f.hidden_field :status, :value => 'Created'
= f.hidden_field :email_type, :value => 'newsletter'
= f.text_area :content, :class => 'editor'

%br

= f.submit 'Save', :class => 'buttons'
|
= link_to 'Cancel', :back

