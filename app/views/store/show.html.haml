- content_for :left_column do
	%h1
		= @sku.title
		
	= @sku.description
	.price
		Price:
		= @sku.price
		= link_to "Buy Now", new_author_order_path( @current_author, :sku => @sku )

	.items
		Contains:
		- for item in @sku.items
			%div[item]= item.title
			
- content_for :right_column do
	&nbsp;