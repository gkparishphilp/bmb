- content_for :left_column do

	- for sku in @skus
		- if sku.is_merch?
			= render 'item_listing', :item => sku.merches.first, :sku => sku
		- else
			= render 'item_listing', :item => sku.book, :sku => sku
		
- content_for :right_column do
	&nbsp;