%h2
	Edit
	= @book.title
	
= image_tag @book.avatar.location('thumb')

= form_for [ @author, @book ], :html => { :multipart => true } do |f|
	= file_field_tag :attached_avatar_file
	= f.label 'Subtitle:'
	= f.text_field :subtitle
	= f.label "Description:"
	= f.text_area :description
	= f.submit 