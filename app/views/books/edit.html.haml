- content_for :left_column do
	= render :partial => 'admin/nav'
- content_for :middle_column do
	.form_group
		= form_for [ @author, @book ] do |f|
			%h1
				Enter metadata for "
				= @book.title
				"
			= link_to "(Change Title)", "#", :class => :show_edit_title
			#edit_title
				= f.text_field :title, :class => :book_title_field
				= f.submit "Update"
				= link_to "Cancel", "#", :class => :hide_edit_title
		
		= image_tag @book.avatar.location('thumb'), :width => @book.avatar.thumb_width if @book.has_attached_avatar?
		
		%br
		= form_for [ @author, @book ], :html => { :multipart => true } do |f|
			= f.label 'Select Cover Art'
			= file_field_tag :attached_avatar_file
			%br
			= f.label 'Subtitle:'
			= f.text_field :subtitle
			= f.label 'Genre:'
			= f.collection_select :genre_id, @genres, :id, :name
			= f.label "Description:"
			= f.text_area :description, :class => 'editor_small'
			%br
			= f.submit('Update Metadata')
	.clear
		
- content_for :right_column do
	= render 'shared/help'
	
	%hr
	%br
	%h3 
		Tip:
	%p
		After entering the book metadata, add 
		= link_to 'physical', physical_assets_author_book_path(@current_author, @book)
		or 
		= link_to 'digital', digital_assets_author_book_path(@current_author, @book)
		assets for your book.

		

