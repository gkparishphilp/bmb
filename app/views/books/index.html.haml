- content_for :left_column do
	- for book in @books do
		= image_tag( book.avatar.location('thumb'), :width => 64 ) if book.has_attached_avatar?
		%h3= link_to book.title, author_book_path( @author, book )
		%hr

- content_for :right_column do
	&nbsp;