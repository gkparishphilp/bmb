- content_for :left_column do
	- for book in @books do
		= book.avatar.location('thumb') if book.has_attached_avatar?
		= link_to book.title, author_book_path( @current_author, book )
		%hr

- content_for :right_column do
	= render :partial => 'authors/right_column'