- content_for :left_column do
	= render :partial => 'admin/nav'
- content_for :middle_column do
	.form_group
		%h1 New Book
		= form_for [ @author, @book ], :html => { :multipart => true } do |f|
			= render 'form', :f => f, :m => 'Create Book'
